# Contracts: Change Scene Plot Without Dragging

This feature introduces no new API route. It reuses the existing scene move endpoint and formalizes expected behavior for non-drag plot changes initiated from:

- scene action controls
- scene form plot selector

Detailed request/response and shift semantics are documented in `scene-plot-change.md`.
